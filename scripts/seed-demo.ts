// Disabled for A A Tech — no demo data. Real employees are invited via
// /admin/employees once the first admin is bootstrapped.

console.log("Demo seed disabled — A A Tech starts with empty data.");
process.exit(0);
